{% extends 'base.html.twig' %}

{% block title %}{% trans %}Squadron Members{% endtrans %}{% endblock %}

{% block content %}
    <div class="content">
        <div class="row">
            {% for member in members %}
                {% if loop.index0 is divisible by(3) %}
        </div>
        <div class="row">
                {% endif %}
            <div class="col-md-4">
                <!-- Widget: user widget style 1 -->
                <div class="box box-widget widget-user">
                    <!-- Add the bg color to the header using any of the bg-* classes -->
                    <div class="widget-user-header bg-dark">
                        <h4 class="widget-user-username">{% trans with {'%name%': member.commanderName} %}CMDR %name%{% endtrans %}</h4>
                        <h5 class="widget-user-desc"><i class="ed-asset ed-asset-icon-squadron-rank-{{ member.customRank.orderId+1 }}"></i> {{ member.customRank.name|default(member.rank.name)|trans }}</h5>
                    </div>
                    <div class="widget-user-image">
                        <img class="img-responsive img-circle" src="{{ member.avatarImgUrl }}" alt="User Avatar">
                    </div>
                    <div class="box-footer">
                            <ul class="nav">
                                <li>{% trans %}Joined{% endtrans %} <span
                                            class="pull-right">{{ member.dateJoined|ago }}</span></li>
                                <li>{% trans %}Total Earning{% endtrans %} <span
                                            class="pull-right">{{ total_earned[member.id]|localizednumber }}</span></li>
                                <li>{% trans %}Earning Rank{% endtrans %} <span
                                            class="pull-right">{{ rank[member.id] }}</span></li>
                            </ul>
                    </div>
                </div>
                <!-- /.widget-user -->
            </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}